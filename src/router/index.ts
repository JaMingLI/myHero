// Note: AppRoutes is intentionally NOT exported here to avoid circular dependencies.
// Import it directly from "@/router/routes" where needed (e.g., main.tsx).
export { PATHS, type PathKey, type Path } from "./paths";
